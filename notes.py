"""
# Docker in Data Science

Download docker : can be used from GUI & CMD

commands 

docker ps -a : display all images & info

docker run -it ubuntu : it will pull the ubuntu image run the container 
and insert user inside the shell to work interactively.

Docker Scout : To check & fix vulnerabilities in an image.

Docker Init : To containerize your application

DATA Science : Docker handles dependencies issue

Container : a way to package application with all the necessary
            dependencies & configuration
portable artifact, easily share & move this package to any environment
makes dev & deployment more easy and efficient

Docker works on virtualizing application layer, OS kernel layer is untouched

VM touches application layer as well as OS kernel

Docker Compose : Multi stack applications


"""